{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/main",
            "args": ["-c","/fileserver/config/config.cfg"],
            "cwd": "${workspaceFolder}"
        }
    ],
    "version": "2.0.0"
}