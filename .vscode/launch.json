{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/Build/Cmd/fileserver",
            "args": ["-c","/fileserver/config/config.cfg"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ],
    "version": "2.0.0"
}