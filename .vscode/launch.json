{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/Build/Cmd/fileservercmd",
      "args": [
        "-c",
        "Config/Config.cfg"
      ],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
    }
  ],
  "version": "2.0.0"
}